A - 1:
Problem 1: Temperature Converter

Scenario: Write a program that converts temperature from Fahrenheit to Celsius.
If the calculated Celsius temperature is above 30, print "It's hot!", otherwise print "It's cool!".
Example: If a user enters 86, the program should convert it to Celsius (30 degrees Celsius) and output: "It's hot!".

fahrenheit = int(input("Enter the fahrenheit to check Temperature"))
def fahrenheit_to_celsius(fahrenheit):
    celsius = (fahrenheit - 32) / 1.8
    if celsius >=30:
        print("It's Hot!")
    else:
        print("It's cool!")
    return celsius
fahrenheit_to_celsius(fahrenheit)

Problem 2: Basic Authentication System

Scenario: Create a simple authentication system where the user provides a username and password. 
If the username is "admin" and the password is "1234", print "Access granted", otherwise print "Access denied".


username=input("Enter the username")
password=input("Enter the password")

if username == 'admin':
    if password =='1234':
        print("Access granted!")
    else:
        print("Access denied!")
else:
    print("Access denied!")

Problem 3: Grade Calculator

Scenario: Write a program that calculates the final grade of a student based on their score. 
If the score is 90 or above, it's an 'A', between 80 and 89 is a 'B', between 70 and 79 is a 'C', between 60 and 69 is a 'D', and below 60 is an 'F'.

Example: If a student enters a score of 85, the program should output: "Your grade is B".

score = int(input("Enter the student score"))

if score >=90:
    print("Grade is 'A'")
elif score >=80 and score <=89:
    print("Grade is 'B'")
elif score >=70 and score <=79:
    print("Grade is 'C'")
elif score >=60 and score <=69:
    print("Grade is 'D'")
elif score <60:
    print("Fail")

Problem 4: Age Group Classifier

Scenario: Develop a program that categorizes a person's age group based on their age. 
If the age is 13 or below, they are a "child", if it's between 14 and 19, they are a "teenager", 
if it's between 20 and 65, they are an "adult", otherwise, they are a "senior".

Example: If a person enters an age of 34, the output should be: "You are an adult".

age = int(input("Enter the age of the child"))

if age <=13:
    print("Child")
elif age>= 14 and age <=19:
    print("teenager")
elif age >=20 and age <=65:
    print("adult")
else:
    print("Senior")

Problem 5: Discount Calculator

Scenario: Write a program that applies a discount to a purchase based on the amount.
If the purchase amount is more than $100, apply a 10% discount. Display the original price, 
the discount amount, and the final price.

Example: If a user enters an amount of $120, the output should be something like: "Original price: $120, 
Discount: $12, Final price: $108".

amount = int(input("Enter the amount for the purchase"))
discount_amt = amount*0.1
final_price = amount-discount_amt

if amount<100:
    print("The total purchase amount is",amount)
elif amount>100:
    print("The original price is : ",amount)
    print("The discount amount is :",discount_amt)
    print("The final price is :",final_price)

A - 2:

1. Grade Calculator


Scenario: Create a program that calculates the final grade of a student based on their scores in various assessments. 
The program should ask for the student's name, the scores for assignments, midterm, and final exam.
 Then, it should calculate the total score based on weighted averages 
 (e.g., assignments 30%, midterm 30%, final 40%) and assign a letter grade (A, B, C, D, F) based on the total score.


stud_name = input("Enter the student name")
score=int(input("Enter the score for assignments "))
midterm = int(input("Enter the midterm score  "))
final_exam= int(input("Enter the finalexam score "))

calculate = score+midterm+final_exam
if calculate>=90:
    print("Garde is A")
elif calculate>=80 and calculate <89:
    print("Grade is B")

elif calculate>=70 and calculate <79:
    print("Grade is C")
elif calculate>=60 and calculate<69:
    print("Grade is D")
elif calculate<59:
    print("Grade is F")

2. Simple Banking Application

Scenario: Develop a simple banking application where a user can start with an initial balance and then choose to either deposit or withdraw money. 
The program should ask for the user's initial balance, then present options to deposit or withdraw. 
After the transaction, it should display the updated balance. 
Ensure the application does not allow a withdrawal that would lead to a negative balance.

initalbal = float(input("Enter the initial bal for your account"))

option=input("Enter the option for withdraw or deposit")

if option =="withdraw":
    withdraw = float(input("Enter the withdraw amount from your account"))
    total=initalbal - withdraw
    view=input("Do you want to see the total amount")
    if view == "yes":
        print("Total remaining bal after withdrawing is :",total)
    elif view == "no":
        print("Thankyou, Have a good day!")


elif option == "deposit":
    deposit = float(input("Enter the deposit amount"))
    total = initalbal + deposit
    view = input("Do you want to see the total amount")
    if view == "yes":
        print("Total remaining bal after withdrawing is :", total)
    elif view == "no":
        print("Thankyou, Have a good day!")

print("Thankyou for the transaction in Ranjith Bank..!!!")



3. Unit Converter

Scenario: Write a program that converts units between different systems. For example, 
a simple temperature converter from Celsius to Fahrenheit and vice versa. 
The program should ask the user which conversion they want to perform and then ask for the temperature to convert.


Fahrenheit = int(input("Enter the Fahrenheit"))
Celsius = Fahrenheit - 32 / 8

converter=input("Which temperature converter you would like to use > from Celsius to Fahrenheit ")
tem = float(input("Enter the temperature"))
#Celsius to Fahrenheit: (9/5)*(C+32)
#To convert from Fahrenheit to Celsius:(5/9)*(F-32)

if converter == "Celsius":
    F=(9/5)* tem + 32 
    print(F)
elif converter == "Fahrenheit":
    C=(5 / 9) * (tem - 32)
    print(C)
else:
    print("Your output!, thankyou :)")



4. Age Group Classifier

Scenario: Design a program that categorizes people into different age groups based on their age. 
For example, 'Child' (< 13), 'Teenager' (13-19), 'Adult' (20-65), and 'Senior' (> 65). 
The program should ask for the person's age and then print out the corresponding age group.

age = int(input("Enter the age of the child"))

if age < 13:
    print("Child")
elif age >= 13 and age <=19:
    print("teenager")
elif age >=20 and age <=65:
    print("adult")
else:
    print("Senior")

5. Event Ticket Pricing

Scenario: Create a program for an event ticketing system where the ticket price varies based on 
the age of the attendee and whether they are a student. 
The base ticket price is a fixed amount, but children under 12 get a 50% discount,
seniors over 65 get a 30% discount, and students get a 20% discount regardless of age. 
The program should ask for the attendee's age and if they are a student, 
then calculate and display the ticket price.

baseticket= 5000
age = int(input("Enter the age of the person"))
detail= input("Are you student or working professional")
if age <12:
    price=baseticket*0.5
    print("Ticket for your child is: ",price)
elif age >=65:
    price=baseticket*0.3
    print("Ticket for senior is :", price)
elif detail == "student":
    price = baseticket * 0.2
    print("Ticket for student is :", price)
else:
    print("Your ticket price is : ",baseticket)

A - 3:

Problem 1: Azurelib Academy wants their students to be healthy thus 
you are asked to develop a simple program to track the number of miles a user has run each day for a week. 
The program should prompt the user to enter the number of miles they ran each day
,calculate the total miles run for the week, and display the average miles run per day.
Implement this program using Python variables, data types,
 and a while loop with the input function to take input from the user.
 


#the user to enter the number of miles they ran each day

for i in range(7):
    eachday = float(input("Enter the miles each day"))
    days = []

    if i == 0:
        monday=eachday
        print("monday",eachday)
    elif i == 1:
        tuesday =eachday
        print("tuesday", eachday)
    elif i == 2:
        wednesday =eachday
        print("wednesday", eachday)
    elif i == 3:
        thursday = eachday
        print("thursday", eachday)
    elif i ==4:
        friday = eachday
        print("friday", eachday)
    elif i == 5:
        saturday = eachday
        print("saturday", eachday)
    elif i == 6:
        sunday = eachday

        print("sunday", eachday)

#calculate the total miles run for the week
total_miles = monday + tuesday + wednesday + thursday + friday + saturday + sunday
print("Total miles for the week:",total_miles)

#display the average miles run per day
avgr = total_miles/7
print("Average miles runs per day is :",avgr)

Problem 2: Write a simple inventory management system for a small store. 
How would you use Python variables, data types, and a while loop to allow the user 
to input the quantity of items sold each day for a week, calculate the total sales for the week, 
and display the average daily sales?

i=0
daily_sold=[]
daily_quantity= []

while i <7:
    item = input("Enter the item sold  ")
    daily_sold.append(item)
    quantity = int(input("Enter the item sold"))
    daily_quantity.append(quantity)
    i+=1
print("item sold for the week",daily_sold)
total_Sale= sum(daily_quantity)
avgr= total_Sale / 7
print("sold for the week",total_Sale)
print("avg sale for the week",avgr)


Problem 3: You're tasked with creating a Python program to track the deposits made into a bank account 
over ten transactions. 
Using a while loop, prompt the user to input the amount deposited for each transaction.
After all ten transactions are complete, 
calculate the total amount deposited into the account and 
display it to the user.

i=0
total_deposit = []
while i <10:
    deposit = int(input("Enter the deposit amount"))
    total_deposit.append(deposit)
    i+=1
total= sum(total_deposit)
print("Total deposited amount is :",total)
    
